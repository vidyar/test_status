language: python
python:
 - 2.6
 - 2.7
install: echo "Nothing to install"
before_script: echo $VIRTUALENV_PYTHON
script:
 - if ["$VIRTUALENV_PYTHON" == "2.6"] echo "bail out now"
 - echo "Waiting for 10 seconds"
 - sleep 10
